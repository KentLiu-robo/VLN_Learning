# Assets

Use this folder for images and small attachments referenced by notes.

Suggested layout:

```text
assets/
├── images/
└── attachments/
```

Avoid committing large binary files. For papers, datasets, and model files, store links or metadata instead.
