# Experiments

Use this folder for small scripts, notebooks, or reproduction notes that support the learning vault.

Large datasets, checkpoints, generated outputs, and model files should stay out of Git and are ignored by default.
